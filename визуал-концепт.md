# Визуальная концепция

Источник для цветовых переменных и типографики в `index.html`. При обновлении палитры
или шрифтов — сверяться с этим файлом; при изменении концепции — обновлять сначала здесь,
потом переносить в CSS.

## Цветовая палитра (правило 60/30/10)

- **Основной фон, 60–70%** — слоновая кость `#FDFBF7`.
- **Текст и контуры, 20–30%** — глубокий графитовый `#2B2B2B` (ориентировочный тон, можно
  скорректировать по референсу).
- **Акцент для кнопок и плашек, 10%** — глубокий винный/бордо `#5E1B28` (тоже ориентировочный —
  оттенков бордо много, этот подобран «на глаз», можно уточнить по конкретному образцу).

## Типографика

- **Заголовки (хуки, манифест)** — Georgia, элегантный засечковый шрифт.
- **Основной текст и подзаголовки** — Onest.

## Соответствие текущим переменным в index.html

Текущий `:root` (index.html:8–16) на терракотовой палитре, её нужно будет заменить:

| Переменная | Сейчас | Станет |
|---|---|---|
| `--accent` | `#b5652f` (терракота) | бордо `#5E1B28` |
| `--accent-dark` | `#8f4e22` | более тёмный бордо |
| `--ink` | `#2b2420` (тёплый коричневый) | графит `#2B2B2B` |
| `--paper` | `#faf6f1` | слоновая кость `#FDFBF7` |
| `--paper-alt` | `#f1e9df` | пересмотреть под новую палитру |
| `--muted` | `#6b5f54` | пересмотреть под графит |

Шрифты:
- Заголовки (`h1, h2, h3`, index.html:31) и цитата манифеста (index.html:292) уже на
  `Georgia, "Times New Roman", serif` — менять не нужно, концепция уже совпадает.
- Основной текст `body` (index.html:24) сейчас на системном sans-serif стеке
  (`-apple-system, "Segoe UI", Roboto...`) — нужно заменить на Onest. Onest не системный
  шрифт, для него понадобится подключение через Google Fonts (`<link>` в `<head>`) или
  самостоятельный хостинг `.woff2`-файлов.
